{
  "overview": null,
  "name": "ui.igGridColumnFixing",
   "title": "Column Fixing",
  "category": null,
  "visual": true,
  "events": {
    "columnFixed": {
      "cancellable": false,
      "args": [
        {
          "name": "evt",
          "type": "Event",
          "optional": false
        },
        {
          "name": "ui",
          "type": "Object",
          "optional": false
        }
      ],
      "description": {
		"en": "Event which is fired when column fixing operation is finished<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the fixed column<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the fixed column<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget",
		"jp": "Event which is fired when column fixing operation is finished<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the fixed column<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the fixed column<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget"
	}
    },
    "columnFixing": {
      "cancellable": true,
      "args": [
        {
          "name": "evt",
          "type": "Event",
          "optional": false
        },
        {
          "name": "ui",
          "type": "Object",
          "optional": false
        }
      ],
      "description": {
		"en": "Event which is fired when column fixing operation is initiated<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the column to be fixed<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the column to be fixed<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget",
		"jp": "Event which is fired when column fixing operation is initiated<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the column to be fixed<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the column to be fixed<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget"
	}
    },
    "columnFixingRefused": {
      "cancellable": false,
      "args": [
        {
          "name": "evt",
          "type": "Event",
          "optional": false
        },
        {
          "name": "ui",
          "type": "Object",
          "optional": false
        }
      ],
      "description": {
		"en": "Event which is fired when column fixing operation is initiated but sum of the width of the fixed columns container and width of the column to be fixed exceeds the grid width<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the column to be fixed<br>&nbsp;&nbsp;&nbsp;use args.isGroupHeader to get whether header cell is of type multicolumn (when multicolumn headers are defined)<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget",
		"jp": "Event which is fired when column fixing operation is initiated but sum of the width of the fixed columns container and width of the column to be fixed exceeds the grid width<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the column to be fixed<br>&nbsp;&nbsp;&nbsp;use args.isGroupHeader to get whether header cell is of type multicolumn (when multicolumn headers are defined)<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget"
	}
    },
    "columnUnfixed": {
      "cancellable": false,
      "args": [
        {
          "name": "evt",
          "type": "Event",
          "optional": false
        },
        {
          "name": "ui",
          "type": "Object",
          "optional": false
        }
      ],
      "description": {
		"en": "Event which is fired when column unfixing operation is done<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the unfixed column<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the unfixed column<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget",
		"jp": "Event which is fired when column unfixing operation is done<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the unfixed column<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the unfixed column<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget"
	}
    },
    "columnUnfixing": {
      "cancellable": true,
      "args": [
        {
          "name": "evt",
          "type": "Event",
          "optional": false
        },
        {
          "name": "ui",
          "type": "Object",
          "optional": false
        }
      ],
      "description": {
		"en": "Event which is fired when column unfixing operation is initiated<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the column to be unfixed<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the column to be unfixed<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget",
		"jp": "Event which is fired when column unfixing operation is initiated<br>&nbsp;&nbsp;&nbsp;use args.columnKey to get the column key of the column to be unfixed<br>&nbsp;&nbsp;&nbsp;use args.columnIndex to get the column index of the column to be unfixed<br>&nbsp;&nbsp;&nbsp;use args.owner to get a reference to the widget"
	}
    }
  },
  "properties": {
    "columnSettings": {
      "type": "array",
      "defaultValue": null,
      "description": {
		"en": "A list of column settings that specifies custom column fixing options on a per column basis",
		"jp": "A list of column settings that specifies custom column fixing options on a per column basis"
	},
     "schema": {
		"allowFixing": {
          "type": "bool",
          "defaultValue": true,
          "description": {
		"en": "Defines if the column is allowed to be fixed",
		"jp": "Defines if the column is allowed to be fixed"
	}
        },
        "columnIndex": {
          "type": "number",
          "defaultValue": null,
          "description": {
		"en": "Defines the index of the column for fixing",
		"jp": "Defines the index of the column for fixing"
	}
        },
        "columnKey": {
          "type": "string",
          "defaultValue": null,
          "description": {
		"en": "Defines the key of the column for fixing",
		"jp": "Defines the key of the column for fixing"
	}
        },
		"isFixed": {
          "type": "bool",
          "defaultValue": false,
          "description": {
		"en": "Defines if the column is fixed",
		"jp": "Defines if the column is fixed"
	}
        }
      }
    },
    "featureChooserTextFixedColumn": {
      "type": "string",
      "defaultValue": "",
      "description": {
		"en": "Feature chooser text of the fixed column",
		"jp": "Feature chooser text of the fixed column"
	},
      "valueOptions": null
    },
    "featureChooserTextUnfixedColumn": {
      "type": "string",
      "defaultValue": "",
      "description": {
		"en": "Feature chooser text of the unfixed column",
		"jp": "Feature chooser text of the unfixed column"
	},
      "valueOptions": null
    },
    "fixingDirection": {
      "type": "string",
      "defaultValue": null,
      "description": {
		"en": " configure on which side to render fixed area",
		"jp": " configure on which side to render fixed area"
	},
      "valueOptions": [
        {
          "name": "left",
          "type": "string",
          "description": {
		"en": " fixed column are rendered on the left side of the main grid.",
		"jp": " fixed column are rendered on the left side of the main grid."
	}
        },
        {
          "name": "right",
          "type": "string",
          "description": {
		"en": " fixed column are rendered on the right side of the main grid.",
		"jp": " fixed column are rendered on the right side of the main grid."
	}
        }
      ]
    },
    "fixNondataColumns": {
      "type": "bool",
      "defaultValue": true,
      "description": {
		"en": "Specify initial fixing of non data columns(like specific rowSelectors columns on the left side of the grid) when fixingDirection is left",
		"jp": "Specify initial fixing of non data columns(like specific rowSelectors columns on the left side of the grid) when fixingDirection is left"
	},
      "valueOptions": null
    },
    "headerFixButtonText": {
      "type": "string",
      "defaultValue": "",
      "description": {
		"en": "Specifies altering text on column fixing header icon when column is not fixed",
		"jp": "Specifies altering text on column fixing header icon when column is not fixed"
	},
      "valueOptions": null
    },
    "headerUnfixButtonText": {
      "type": "string",
      "defaultValue": "",
      "description": {
		"en": "Specifies altering text on column fixing header icon when column is fixed",
		"jp": "Specifies altering text on column fixing header icon when column is fixed"
	},
      "valueOptions": null
    },
    "minimalVisibleAreaWidth": {
      "type": "string",
      "defaultValue": 30,
      "description": {
		"en": " minimal visible area for unfixed columns. For instance if you fix a column(or columns) and the width of the fixed columns is such that the width of visible are of unfixed columns is less than this option then fixing will be canceled",
		"jp": " minimal visible area for unfixed columns. For instance if you fix a column(or columns) and the width of the fixed columns is such that the width of visible are of unfixed columns is less than this option then fixing will be canceled"
	}     
    },
    "scrollDelta": {
      "type": "number",
      "defaultValue": 40.0,
      "description": {
		"en": "option to configure scroll delta when scrolling with mouse wheel or keyboard in fixed columns area",
		"jp": "option to configure scroll delta when scrolling with mouse wheel or keyboard in fixed columns area"
	},
      "valueOptions": null
    },
    "showFixButtons": {
      "type": "bool",
      "defaultValue": true,
      "description": {
		"en": "option to show column fixing buttons in header cells",
		"jp": "option to show column fixing buttons in header cells"
	},
      "valueOptions": null
    },
    "syncRowHeights": {
      "type": "bool",
      "defaultValue": true,
      "description": {
		"en": "option enable syncing heights of rows between fixed/unfixed rows",
		"jp": "option enable syncing heights of rows between fixed/unfixed rows"
	},
      "valueOptions": null
    }
  }
}